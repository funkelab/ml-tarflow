# Tarflow

This repository is a fork of https://github.com/apple/ml-tarflow, which

* turns the code into an installable python package
* adds support for non-image data
* adds support for continuous conditioning variables
